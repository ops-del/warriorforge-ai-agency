"use strict";
// Placeholder for future Stripe integration.
// To integrate Stripe, install the official SDK (npm install stripe) and add STRIPE_SECRET_KEY
// plus webhook signing secrets to the environment. Then, instantiate a Stripe client and create
// Checkout Sessions or PaymentIntents inside the /api/pay endpoint.
Object.defineProperty(exports, "__esModule", { value: true });
exports.stripePlaceholder = stripePlaceholder;
function stripePlaceholder() {
    return { message: "Stripe integration coming soon" };
}
